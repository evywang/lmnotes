{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认权限：核心能力 + 对话框（message/confirm 标题显示应用名；whisper/ffmpeg sidecar 由 Rust 侧 tokio::process 直接 spawn，无需 JS shell 权限）。quick-capture 为 v0.7 全局快捷键浮窗（FR-CAP-01）：JS 侧需要 hide/set-focus；快捷键注册走 Rust GlobalShortcutExt，不经 JS 权限。",
  "windows": ["main", "quick-capture"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-set-focus",
    "dialog:default",
    "dialog:allow-confirm"
  ]
}
