{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认权限：核心能力 + 对话框（message/confirm 标题显示应用名；whisper/ffmpeg sidecar 由 Rust 侧 tokio::process 直接 spawn，无需 JS shell 权限）",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-confirm"
  ]
}
