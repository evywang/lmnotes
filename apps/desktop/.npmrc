# 项目本地 .npmrc。
#
# 已知问题：用户全局 ~/.npmrc 设有 `os=linux`，会导致本机 Windows 环境下
# rollup 原生二进制按 Linux 解析、本地 `npm run build` 失败。
# 修复：若你在此机做前端开发，临时执行
#     npm config set os win32 --location=user
# 或在此文件加一行 `os=win32`（但会破坏 Linux/macOS CI，故未默认启用）。
# CI 在 GitHub Actions runner 上能正确自检平台，不受此影响。
